- name: variables from files
  hosts: localhost
  tasks:
  - name: print information
    vars:
      Timings: "7:15AM IST"
    ansible.builtin.debug:
      msg: "Hi we are learning {{Course}} from trianer {{Trainer}} Duration is {{Duration}} Timings are: {{Timings}}."